#!/bin/bash

# Change to directory that holds this script
cd $(dirname $(readlink -f ${0}))

if [ ! -f started ]; then
  echo "Container is not running..."
  exit 1
fi

./killprocs.sh
umount union
rm started
