source :rubygems

gem 'i18n' # Active support apparently requires this but it isn't in the Gemfile??
gem 'active_support'
gem 'nokogiri', '>= 1.4.4'
gem 'rake'
gem 'yajl-ruby', '>= 0.7.9'

gem 'vcap_common'

group :test do
  gem 'rspec'
end
