check process mongodb-<%= @app_name %> with pidfile <%= @config[:pidfile] %>
  start = "/etc/init.d/mongodb-<%= @app_name %> start" as uid root and gid root
  stop  = "/etc/init.d/mongodb-<%= @app_name %> stop"
